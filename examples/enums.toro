enum Message {
    text(string)
    quit
}

function process(message: Message) {
    handle message {
        text(value) {
            print(value)
        }

        quit {
            return
        }
    }
}

function main() {
    message := Message.text("hello")
    quit := Message.quit
    process(message)
    process(quit)
}
