enum Message {
    text(string)
    quit
}

function process(message: Message) {
    handle message {
        text(value) {
            print(value)
        }

        quit {
            return
        }
    }
}
